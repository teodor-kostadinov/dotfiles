# Teodor's Dotfiles

## Install

```bash
cd; git clone https://github.com/teodor-kostadinov/dotfiles .dotfiles; cd ./.dotfiles/setup.sh
```

## Shortcuts

- `OPTION` + `SPACE` = Alfred
- `OPTION` + `i` = Siri
- `OPTION` + `d` = Dash
